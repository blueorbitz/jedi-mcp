# Model Provider Configuration
# Choose which AI provider to use: "gemini" or "bedrock"
JEDI_MODEL_PROVIDER=gemini

# Google Gemini Configuration (required if using gemini provider)
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-api-key-here

# Optional: Override specific model IDs
# Navigation extraction model (default: gemini-2.0-flash-exp for Gemini, qwen.qwen3-coder-30b-a3b-v1:0 for Bedrock)
# JEDI_NAVIGATION_MODEL=gemini-2.0-flash-exp

# Content processing model (default: gemini-2.0-flash-exp for Gemini, qwen.qwen3-coder-30b-a3b-v1:0 for Bedrock)
# JEDI_CONTENT_MODEL=gemini-2.0-flash-exp

# AWS Bedrock Configuration (required if using bedrock provider)
# Configure AWS credentials via standard methods:
# - Environment variables: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION
# - AWS credentials file (~/.aws/credentials)
# - IAM role (if running on AWS infrastructure)

# Vector Search Configuration
# Embedding model for vector search (default: all-MiniLM-L6-v2)
# This setting is used during the 'generate' phase and stored in the database
# The same model will be automatically loaded during the 'serve' phase for consistency

# Available embedding models:
# - all-MiniLM-L6-v2: 384 dimensions, good quality, fast processing, recommended default
# - Qwen/Qwen3-Embedding-0.6B: 1024 dimensions, higher quality, slower processing, larger storage
#
# Note: Using an unsupported model will result in an error:
# "Unsupported model: <model_name>. Supported models: {'all-MiniLM-L6-v2', 'Qwen/Qwen3-Embedding-0.6B'}"
JEDI_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Advanced Vector Search Settings (optional)
# Maximum text length for embedding generation (default: 8000)
# Texts longer than this will be automatically truncated
# JEDI_EMBEDDING_MAX_LENGTH=8000

# Batch size for embedding generation (default: 100)
# Larger batches are more efficient but use more memory
# JEDI_EMBEDDING_BATCH_SIZE=100

# Vector search similarity threshold (default: 0.7)
# Higher values return more similar results, lower values return more diverse results
# JEDI_VECTOR_SIMILARITY_THRESHOLD=0.7

# Enable hybrid search combining vector and keyword search (default: true)
# Hybrid search provides better results by combining semantic and lexical matching
# JEDI_ENABLE_HYBRID_SEARCH=true
